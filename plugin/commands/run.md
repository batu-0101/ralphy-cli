---
description: Run the cross-platform Ralphy autonomous coding loop for a PRD or task list.
argument-hint: "--prd <file> [--claude|--grok] [options]"
---

# Run Ralphy

Read the `ralphy-orchestrator` skill completely, then run the installed `ralphy`
CLI with the user's arguments. Preview with `--dry-run` before a mutating run
unless the user already supplied it.

Requested arguments: `$ARGUMENTS`
